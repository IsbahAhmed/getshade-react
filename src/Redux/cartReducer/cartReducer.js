var initialState = []
var cartReducer = (state = initialState,actions)=>{
    var {payload,type} = actions;
    switch (type) {
       
    
        default:
          return state;
    }
}
export default cartReducer